# =============================================================================
# Frontend Environment Configuration
# Windows/WAMP Version
# =============================================================================
#
# Copy this file to .env and update the values according to your setup.
# DO NOT commit .env to version control!
#
# =============================================================================

# ---------------------------------------------------------------------------
# API Configuration
# ---------------------------------------------------------------------------
# Backend API URL
# Default: http://localhost:8000/api/v1/
VITE_API_URL=http://localhost:8000/api/v1/

# WebSocket URL for real-time notifications
# Default: ws://localhost:8000/ws/
VITE_WS_URL=ws://localhost:8000/ws/

# ---------------------------------------------------------------------------
# Map Configuration
# ---------------------------------------------------------------------------
# MapLibre style URL
# Default: OpenStreetMap tiles via MapLibre demo
VITE_MAP_STYLE_URL=https://demotiles.maplibre.org/style.json

# Alternative: OpenStreetMap Vector Tiles (requires API key for production)
# VITE_MAP_STYLE_URL=https://api.maptiler.com/maps/streets/style.json?key=YOUR_KEY

# Default map center (Philippines)
VITE_MAP_CENTER_LAT=12.8797
VITE_MAP_CENTER_LNG=121.7740
VITE_MAP_DEFAULT_ZOOM=6

# ---------------------------------------------------------------------------
# Application Settings
# ---------------------------------------------------------------------------
# Application name displayed in browser tab and UI
VITE_APP_TITLE=Project Tracking Management System

# Application version
VITE_APP_VERSION=2.0.0

# ---------------------------------------------------------------------------
# Feature Flags
# ---------------------------------------------------------------------------
# Enable WebSocket connection for real-time updates
# Set to false if WebSocket/Redis is not configured
VITE_ENABLE_WEBSOCKET=true

# Enable analytics (e.g., Google Analytics)
# Set to true and configure tracking ID to enable
VITE_ENABLE_ANALYTICS=false

# ---------------------------------------------------------------------------
# Build Settings
# ---------------------------------------------------------------------------
# Build mode: development | production
VITE_BUILD_MODE=development

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
#
# 1. Port Configuration:
#    - Default backend port: 8000
#    - Default frontend port: 3000
#    - If ports are in use, update this file accordingly
#
# 2. Windows Firewall:
#    - Allow Node.js and Python through Windows Firewall
#    - Allow ports 3000 and 8000 if needed
#
# 3. CORS Configuration:
#    - Ensure backend .env has CORS_ALLOWED_ORIGINS with port 3000
#    - Example: CORS_ALLOWED_ORIGINS=http://localhost:3000
#
# 4. Available Scripts:
#    - npm run dev       # Start development server
#    - npm run build     # Build for production
#    - npm run preview   # Preview production build
#    - npm run lint      # Run ESLint
#    - npm run typecheck # Run TypeScript checks
#
# =============================================================================
